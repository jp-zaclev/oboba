{# templates/projet/edit.html.twig #}
{% extends 'layout/project_layout.html.twig' %}

{% set container_margin = false %} {# Pas de mt-4 ici #}

{% block title %}Modifier le projet - {{ projet.nom }}{% endblock %}

{% block page_content %}
    <h1 class="text-white">Modifier le projet : {{ projet.nom }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="mt-3">
            <button type="submit" class="btn btn-primary me-2">Enregistrer</button>
            <a href="{{ path('projet_list') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    {{ form_end(form) }}
{% endblock %}
