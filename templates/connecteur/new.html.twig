{# templates/connecteur/new.html.twig #}
{% extends 'layout/project_layout.html.twig' %}

{% set container_margin = false %} {# Pas de mt-4 ici #}

{% block title %}Ajouter un connecteur - {{ projet.nom }}{% endblock %}

{% block page_content %}
    <h1 class="text-white">Ajouter un connecteur pour {{ projet.nom }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="mt-3">
            <button type="submit" class="btn btn-primary me-2">Ajouter</button>
            <a href="{{ path('connecteur_list', {'projetId': projet.id}) }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    {{ form_end(form) }}
{% endblock %}
